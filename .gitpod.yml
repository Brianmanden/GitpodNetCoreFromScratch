image: gitpod/workspace-dotnet

tasks:
  - name: Restore & Build
    init: |
      dotnet --version
      dotnet dev-certs https 
      dotnet restore
      dotnet build

  - name: BJAtest
    init: echo 'virker ?'
    openMode: split-right

vscode:
  extensions:
    - muhammad-sammy.csharp
